# Example trajectory assets

The example molecular dynamics trajectory assets associated with this repository
(`md.xtc` and `md.tpr`) are maintained separately in the corresponding Zenodo
archive due to GitHub file-size limitations.

These files were used to generate the canonical example output figure included
in:

../example_output/

Please obtain the trajectory assets from the associated Zenodo dataset before
running the example workflow locally.
